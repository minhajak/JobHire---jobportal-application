import React, { type JSX } from "react";
import { NetworkUpdate } from "../../features/networks";

const CatchupPage: React.FC = (): JSX.Element => {
  return <NetworkUpdate />;
};

export default CatchupPage;
